spring:
  application:
    name: ad-eureka
eureka:
  instance:
    hostname: localhost
    prefer-ip-address: false
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8080/eureka/
